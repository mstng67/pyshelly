[tool.poetry]
name = "pyshelly"
version = "0.1.0"
description = "python wrapper for shelly smart devices"
authors = ["mstng67"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
